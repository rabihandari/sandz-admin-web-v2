import React from 'react';
import FormLoadingComponent from '@/components/FormLoadingComponent';

const Loading = () => {
  return <FormLoadingComponent numberOfInputs={1} shouldRenderImageInput />;
};

export default Loading;
